# *图像处理*学习文档
一、OpenCV常用函数用法
1. cv2.imread()	读取图像	img = cv2.imread("test.jpg")
2. cv2.imwrite()	保存图像	cv2.imwrite("out.jpg", img)
3. cv2.imshow()	显示图像	cv2.imshow("win", img)
4. cv2.waitKey()	等待按键	cv2.waitKey(0)
5. cv2.destroyAllWindows()	关闭窗口	—
6. cropped = img[y1:y2, x1:x2]图像裁剪
7. dst = cv2.convertScaleAbs(src, alpha=1.3, beta=30)对比度与亮度调整
8. blur = cv2.GaussianBlur(img, (15, 15), 0)高斯模糊
***
二、人脸检测完整流程
1. 人脸检测原理（Haar Cascade）
    1. 基于 Haar-like 特征
    2. 使用 积分图加速
    3. AdaBoost + 级联分类器
    4. OpenCV 内置训练模型
2. 加载分类器
3. 灰度转换
4. 执行人脸检测
四、效果对比分析
原图	清晰、细节完整	原始数据
裁剪	去除无关区域	聚焦目标
对比度增强	更鲜明	提升检测率
全局高斯模糊	整体柔和	初步隐私
人脸检测	标记人脸位置	验证算法
人脸局部模糊	人脸不可识别	隐私保护 ✅